# ES2 单人 TUI 完整移植计划

## 1. 目标与“100%”定义

源基线固定为 `es2-utf8` 仓库提交 `87bba6b`。完成度以该提交为准，不随上游变化漂移。

“100% 移植”表示：原作所有面向普通玩家且能在单人游戏中成立的地点、NPC、物品、武学、战斗、任务、经济和世界事件，都在 TUI 中具有可操作的等价行为，并通过自动测试或人工验收。仅导入名称和描述不算行为完成。

以下多人或运维功能不直接移植，但必须有明确处置结果：

| 原功能 | 单人版处置 |
| --- | --- |
| 登录、账号、密码、网络会话 | 删除，由单一本地存档取代 |
| `say/tell/whisper/shout/channel` | NPC 对话、环境事件和日志取代 |
| 队伍、玩家师徒、玩家帮派 | NPC 同伴、NPC 师承和剧情声望取代 |
| PVP、跨服、邮件 | 排除；相关奖励移入挑战或委托 |
| 巫师、管理员、在线编辑器 | 排除运行时功能，保留离线内容工具 |
| WebMUD、Telnet 代理 | 排除，由 Ratatui/Crossterm 取代 |
| 重启、清理、网络守护进程 | 排除；需要的时间逻辑进入本地世界时钟 |

排除项必须登记原因和替代方案。未经登记的缺失内容视为未完成。

## 2. 源内容盘点

初始审计结果：

| 类别 | 数量 | 说明 |
| --- | ---: | --- |
| Git 跟踪文件 | 2748 | 包括旧引擎、Mudlib 和 WebMUD |
| Mudlib 文件 | 2333 | 玩家内容与运行框架 |
| LPC `.c` 文件 | 1776 | 房间、NPC、物品、技能、命令等 |
| 房间候选 | 560 | 通过 `inherit ROOM` 等模式识别 |
| NPC 候选 | 307 | 通过 `inherit NPC` 等模式识别 |
| 物品候选 | 451 | M2 按继承链复核；含武器 136、护甲 147、普通/消耗/货币等 168 |
| 技能 | 70 | `mudlib/daemon/skill` |
| 玩家/系统命令 | 130 | `mudlib/cmds` |

主要区域及文件数：

| 区域 | 文件数 | 计划批次 |
| --- | ---: | --- |
| village | 40 | M1 样板区 |
| canyon | 55 | M4 |
| city | 102 | M4 |
| choyin | 128 | M5 |
| chuenyu | 101 | M6 |
| death | 19 | M7 |
| goathill | 36 | M5 |
| green | 65 | M6 |
| latemoon | 210 | M7 |
| oldpine | 80 | M5 |
| sanyen | 44 | M6 |
| snow | 108 | M4 |
| temple | 59 | M4 |
| waterfog | 53 | M6 |
| graveyard/jail | 3 | M7 |
| wiz | 6 | 排除普通游戏内容，单独审计 |
| skill/force/npc 等共享内容 | 55 | M2-M3 |

这些数字是候选数量，不是最终完成数。导入器会生成机器可读目录，并报告动态 LPC 行为，人工复核后形成最终基线。

## 3. 迁移原则

1. **稳定 ID**：内容使用 `/d/...` 派生的字符串 ID，存档不依赖 Rust 枚举序号。
2. **数据与规则分离**：名称、描述、出口、数值和掉落进入数据文件；战斗、任务和命令行为进入 Rust 规则模块。
3. **保留来源**：每个迁移条目记录原 LPC 路径和源提交，能够追溯差异。
4. **先等价后改编**：先记录原行为，再定义单人替代；不静默删减。
5. **可重复导入**：静态 LPC 字段由工具抽取，手工覆盖与生成文件分离。
6. **存档可升级**：每次模型变更提供旧版本迁移，不能直接让已有存档崩溃。
7. **完成度不按代码行计算**：只有结构、行为、验证三个状态全部完成才算 `done`。

## 4. 内容状态模型

每个条目使用以下状态：

- `discovered`：已在源仓库发现。
- `structured`：静态字段已导入，引用已解析。
- `implemented`：动态行为已有单人版实现。
- `verified`：自动测试通过并完成人工玩法检查。
- `excluded`：已记录排除理由和替代方案。
- `blocked`：依赖尚未迁移或源行为无法确定。

区域完成条件：区域内所有条目均为 `verified` 或 `excluded`，且不存在无法到达的房间、悬空出口、无效物品引用和不可完成任务。

## 5. 里程碑

### M0：可玩骨架

状态：已完成。

- Ratatui/Crossterm 事件循环与宽窄布局。
- 精、气、神、修炼、恢复、比试和本地存档。
- 一个改编任务作为架构验证。
- 该骨架不计作原版内容完成度。

### M1：迁移基础与 `village` 样板区

状态：已完成。

- [x] 建立源内容审计与 LPC 静态字段导入器。
- [x] 生成机器可读迁移目录，记录解析告警。
- [x] 将位置改为稳定字符串 ID，数据由嵌入式区域文件加载。
- [x] 导入 `village` 的 26 个原版房间、出口和对象引用。
- [x] 对门、潜水、划船、条件出口等动态行为建立待办项。
- [x] 提供地图连通性、ID 唯一性和引用完整性测试。
- [x] 实现并验证首批 9 项行为标记：湖畔划船、湖心返航/潜水、`road6` 限时隐藏出口。
- [x] 实现并验证剩余 10 项行为标记：双侧同步门、路牌、残缺石书机关、摘瓜与瓜农追讨。

行为状态台账：`migration/overrides/village.json`。20 项全部为 `verified`。

验收：26 个房间在 TUI 中可浏览；所有源路径可追溯；动态行为不会被误标为完成。

### M2：通用物品、装备、经济和状态

状态：已完成。

- [x] 按 LPC 继承链重新审计物品范围，确认 451 个可实例化候选。
- [x] 生成 `migration/catalog/items.json`：稳定 ID、来源、继承、类别、重量、价值、武防、饮食数值和行为告警。
- [x] 分类结果：武器 136、护甲 147、普通物品 102、食物 40、饮水 11、组合物品 10、货币 4、药物 1。
- [x] 登记 70 项动态物品行为和 4 个运行时随机名称，均保持 `structured`。
  - 补充审计发现原目录漏记 10 个 `eat_func` 补品钩子；候选总数不变。
- [x] 用稳定 `ItemId` 与物品实例替换当前原型枚举，支持堆叠、重量、装备槽、耐久与掉落。
  - 451 个源定义与 2 个明确登记的单人适配定义进入嵌入式运行时仓库。
  - 行囊保存稳定实例 ID、数量和耐久；装备按槽位引用实例；地面掉落按地点持久化。
  - 武器伤害、护甲减伤和战斗耐久损耗已接入规则，v1/v2 枚举行囊自动升级。
- [x] 实现金银铜换算、商店、购买、出售、赠予和拾取。
  - 保留原版 `100 文 = 1 两银子`、`100 两银子 = 1 两黄金`、千两银票换算。
  - 瓜农按原价出售西瓜；关外商人是明确的单人适配商店；购买检查钱财和负重，出售按价值、数量及耐久折价。
- [x] 实现食物、饮水、药物、绷带、中毒、醉酒等状态。
  - 40 种食物与 11 种饮品保存剩余份量；饱食和饮水随 tick 衰减并影响恢复。
  - 绷带、金疮药、蛇药、普通毒、蛇毒、醉酒和蒙汗状态接入持久化状态时钟。
  - 8 个 `finish_eat` 食物吃完后保留原版骨头或瓜皮状态；药粉可倒入非空酒水并在饮用时发作。
  - 存档 schema v4 保存物品剩余份量、变形状态、酒水药效和角色状态，v1-v3 自动升级。
- [x] 迁移共享 `/obj` 与 `feature` 中面向玩家的行为。
  - `migration/overrides/items.json` 覆盖 13 个共享动态对象：6 个 `verified`、1 个 `adapted`、2 个按依赖延期、4 个多人/巫师对象明确排除。
  - 化尸粉已在 M8 随通用尸体系统适配；其余延期项仍绑定 M3 的战斗/符咒模型，不会被误报为已实现。

验收：物品候选全部分类；核心命令行为有规则测试；存档往返不丢失实例状态。

### M3：完整武学与战斗

状态：已完成。

- [x] 导入并分类全部 70 个技能、114 个普通招式和 11 位门派掌门。
- [x] 实现稳定技能 ID、等级点数、基础/特殊技能映射及 v4 到 v5 存档升级。
- [x] 实现师承、原版入门条件、请教、练习、秘笈研读和三类修行。
- [x] 实现武器用途、招式命中、防御、招架、闪避、身体部位和精气神损伤。
- [x] 区分 `fight` 与 `kill`，实现认输、逃跑、昏迷、实战经验、潜能、杀气和通缉。
- [x] 接入 9 个技能升级钩子、3 个命中钩子和基础内功的恢复/凝神/回精。
- [x] 审计全部动态技能标记和可调用脚本。
  - `migration/overrides/skills.json` 覆盖 143 个动态标记和 33 个可调用脚本。
  - 20 个独立绝招达到 `verified`；10 个依赖后续区域、同伴、召唤或尸体系统的绝招绑定 M4/M6/M7；3 个重复入口标记为 `alias`。
  - 固定源码中从未被调用的学习/练习倍率和 4 个空 `valid_effect` 有明确 `source_noop` 记录。
- [x] 完成天邪、封山和山烟寺代表性构筑的训练、映射、绝招与战斗回归测试。

验收：70/70 技能有稳定目录；所有 143 个行为标记和 33 个脚本均有可机检处置；代表性门派构筑可完成训练和战斗回归测试。

### M4：北部主干区域

状态：已完成。

区域：`village` 行为收尾、`city`、`snow`、`temple`、`canyon`。

- [x] 修正区域导入器，固定读取 `87bba6b`，支持 LPC 合法空格、嵌套 `__DIR__`、银行和当铺房间。
- [x] 生成 `city` 55、`snow` 38、`temple` 27、`canyon` 27 个房间目录；加上 `village` 后，173 个源房间进入运行时世界。
- [x] 打通 `village`、`city`、`canyon`、`snow`、`temple` 的静态跨区主干，并实现黄石隘口铁索和尚书府院墙移动。
- [x] 建立 `migration/overrides/m4-topology.json`，登记单人适配、源缺陷和当前拓扑阻塞项。
- [x] 导入四区及其跨目录放置的 74 个 NPC 定义；区域目录 schema v4 将去除注释后的 71 条静态映射展开为 103 个实例，藏经楼两组三人随机守卫池各确定化一人后共 61 个运行时定义、105 个实例，并支持同房间多 NPC。
- [x] 接入京城 7 个、雪亭镇 2 个原版商人及其 27 种货品；购买行动绑定具体商人并采用原物品价值。
- [x] 建立 `migration/overrides/m4-npcs.json`，覆盖 158 项 NPC 行为标记与两个缺失源引用。
- [x] 导入 30 个 NPC 的 75 个原版询问主题；57 个为静态回复，其中 21 个已放置 NPC 的 50 个主题接入运行时，共形成 95 个房间主题引用。
- [x] 逐项处置 18 个脚本问答：4 verified、6 adapted、1 source_noop、7 excluded，无 deferred；当前 26 个 NPC 的 60 个主题形成 105 个房间引用。
- [x] 接入魏无极五两实物学费与读书识字请教；实现刘安禄两万实战经验身份门槛、主动死斗和残破刀谱掉落。
- [x] 逐项审计 19 个物品交换候选：8 verified、2 adapted、4 deferred、5 excluded；M4 内交换均已运行，醉汉跨区线索留 M6，跟随/雇佣留 M8。
- [x] 实现黄石峡军师口令、军营许可、黑市与真假印鉴古剑链，以及京城酒楼和尚书府条件入口；条件状态进入存档 schema v7。
- [x] 逐项处置 11 个 NPC 自定义命令：9 verified、1 adapted、1 excluded；商人 list、店小二上楼、守城兵死斗和废园令牌一次性北门通行链均可运行，存档进入 schema v11。
- [x] 处置 9 个 NPC 比试门槛：2 个强制死斗、3 个同门限定、4 个源拒绝；8 个已放置 NPC 可运行，死斗移除状态进入 schema v8。
- [x] 审计 8 个 NPC 师承标记：5 verified、1 deferred、2 excluded；5 个已放置授业 NPC 按同门或学费条件提供 37 项原版课程上限。
- [x] 导入 73 个源 NPC 战斗档案的属性/资源、316 项技能、94 项映射与 21 项初始修正；61 个运行时定义的 105 个实例均可比试或死斗，同房间重复个体按已败数量逐个持久化。
- [x] 分类 25 个 NPC 的 87 条 combat-chat；16 个静态放置定义与 2 个确定化守卫接入文本、法术、内功、反击、投降和无尸体反馈，随机移动与尸体动画均保留 M8 后续批次。
- [x] 导入 55 个 NPC 的 93 件初始携带物；加入藏经楼守卫后，43 个已放置定义的 76 个房间实例在首次死斗后按源掉落共 126 件战利品；M8 会同时生成通用地面尸体，单角色适配维持直接掉落以保留战利品。
- [x] 接入城隍庙香火捐献：拒绝无价值物品，按源价值与灵性概率降低杀气；醉汉的青石村玉佩/蒙汗药链准确归入 M6。
- [x] 完成首批 8 个房间事件处置，覆盖 22 项警告：祭坛与兵器库限时密道、加工厂做工、竹林巨石与书柜、茅山滑倒和藏经楼门槛可运行；北驿道悬空出口明确排除，存档进入 schema v12。
- [x] 区域目录 schema v4 导入 40 个固定源门定义；M4 的 35 项门警告处置为 26 verified、9 excluded，14 对有效门双向共享开闭状态并进入存档 schema v13。
- [x] 处置全部 32 项房间细节警告：32 个房间的 42 条 `item_desc` 可查看，30 条保留文本、12 条门描述绑定实时开闭状态；16 verified、16 adapted。
- [x] 逐项处置去除注释后的 77 条房间对象映射和 110 个源实例：103 个静态 NPC、2 个确定化随机守卫和 5 件固定房间物品全部接入；物品一次性初始化进入存档 schema v14。
- [x] 将醉汉玉佩/蒙汗药链绑定 M6 青石村；固定源复核后将陈天星署名信物纠正绑定到 M7 遗漏区域 `mudlib/u/cloud` 振远镖局，邮筒清理、跟随与雇佣绑定 M8，不在 M4 伪造前置状态。
- [x] 审计并处置四区 60 个房间的 107 项动态行为标记：35 项门、32 项细节和 40 项其他动态警告全部有处置；其余动态项为 12 verified、19 adapted、1 source-noop、1 deferred、7 excluded。
- [x] 完成刘老农父女剧情：小娟救出/护送、原版双奖励与父女离场，以及小娟遇害后刘老农强化死斗；M4 暂沿黑松林单人入口适配，M6 导入绮云镇地牢时迁回来源位置。

验收：147 个 M4 房间、74 个 NPC 定义、77 条房间对象映射、107 项动态房间警告和 158 项 NPC 行为标记均有可机检处置；静态主干、门、细节、事件、战斗、掉落与北部剧情可回归运行。雪亭药铺密室、两间旧经楼和京城悬空目标确认为固定源孤立/缺失内容并排除；M6/M7/M8 依赖已绑定具体里程碑，M4 验收通过。

### M5：西南区域与门派

区域：`oldpine`、`goathill`、`choyin`。

状态：已完成。

- [x] 区域目录 schema v4 导入 119 个房间：黑松山 41、牧羊山 16、乔阴县 62；世界源房间总数达到 292。
- [x] 黑松山与牧羊山按固定源双向接入雪亭；依据两端注释的互相引用恢复黑松山南坡至乔阴北门的单人主干。
- [x] 逐量保留去除注释后的 64 条房间对象映射和 133 个源实例；首批 10 件固定房间物品独立初始化，存档进入 schema v15。
- [x] NPC 目录 schema v4 导入 49 个定义（含全局县城官兵）；46 个已放置定义展开为 123 个实例，静态商店/问答、战斗档案、29 条战斗消息、53 件携带物和 132 条潜在掉落接入通用运行层。
- [x] 首批处置 23 项房间警告：19 个房间的 25 条细节全部可查看；县衙铜门双向运行，草堂/竹篱两项源方向错误明确排除。
- [x] 处置黑松山 29 项脚本警告：随机洞窟/松林解析为真实房间并保留逃生链，九条攀爬路径和藤蔓轻功分支可运行；28 adapted、1 source-noop。
- [x] 处置乔阴首批 9 项事件警告：绝壁藤蔓、云台雷击/限时炉口、南门井水和西街石狮可运行，存档进入 schema v16；丹炉紧凑出口语法修复为通用导入能力。
- [x] 处置最后 12 项房间警告：黑松埋骨/封寨、草堂借书和桃林导航可运行，文人掌门入口在区域收口批次接入；幽兰目标留 M7，玩家目录狮穴出口排除。73/73 房间警告均有处置。
- [x] 处置 3 项 NPC 比试门槛与 1 项死斗门槛：酒楼守卫/县官拒绝、采药老者接受；守卫同伴报官增加通缉，3 verified、1 adapted。
- [x] 逐源审计其余 55 项 NPC 行为标记：连同首批战斗门槛共 59 项达到 14 verified、38 adapted、6 deferred、1 excluded；6 个脚本问答、2 项给予和2项死亡钩子可运行，状态进入存档 schema v18，调度、玩家尸体与 NPC 自动化尸等剩余依赖准确归入 M8。
- [x] 精确审计三地 13 项动态物品标记：12 adapted、1 alias；乔阴随机书名、功德箱、缚仙绳和仙丹，黑松竹哨，以及牧羊山死岩蛭补品均有运行时处置与 `m5-items` 机检台账。
- [x] 补齐三地敌人与区域任务：矮胖土匪可按来源回调召来持久化土匪老大，金银花蛇施加蛇毒，三类岩蛭留下可食用尸体；多人同时战斗压缩为单战斗槽中的同房后续挑战。
- [x] 完成乔阴步玄派入口链：首次向骆云舟拜师开放桃林试炼，连续三次解题后第二次拜师正式入派；半山洞缚仙绳、鹤室、云台和丹炉仙丹链可运行，状态进入存档 schema v19。
- [x] 完成西南三地自动端到端玩法验收；未放置的银岩蛭与大鹏鸟确认无固定源引用，不伪造实例。幽兰跨区已随 M7 接入；普通尸体/腐败、NPC 调度/司法及玩家遗体随后均由 M8 世界生命周期收口。

验收：119 个房间、49 个 NPC 定义、73 项房间警告、59 项 NPC 行为标记和 13 项区域物品标记均有可机检处置；三地主干、区域敌人、来源掉落、黑松事件、牧羊山岩蛭洞窟及乔阴任务/门派链可回归运行。拓扑、NPC 与物品台账均为 `complete`，M5 验收通过。

### M6：东部与共享剧情区域

区域：`chuenyu`、`green`、`sanyen`、`waterfog`。

状态：已完成。

- [x] 区域目录 schema v4 导入 121 个房间：绮云镇 37、青石村 39、三烟寺 18、水烟阁 27；世界源房间总数达到 413。
- [x] 保留 60 条房间对象映射的 105 个源实例；其中 56 条映射解析为 92 个 NPC 实例，3 条映射的 11 件固定物品进入存档 schema v20。
- [x] NPC 目录 schema v4 导入 57 个定义；48 个已放置定义展开为 92 个实例，1 个商人的 6 项货品、10 个固定问答、53 个战斗档案、33 条战斗消息和 81 件携带物进入通用运行层。
- [x] 接通青石村/雪亭与水烟阁/雪亭两组固定源双向主干；绮云镇固定源只有出城方向，单人适配补入不覆盖天驼关道路的返回岔路。
- [x] 登记两项固定源缺陷：三烟寺前院引用不存在的两个 `monk_guard`，水烟阁入口指向不存在的 `guildhall`；均保留原始引用并明确排除运行时伪造。
- [x] 逐源审计并处置 65 项房间行为标记：29 verified、34 adapted、2 excluded；16 个门端点、38 条查看细节、绮云城堡机关/暗箭、青石八卦阵/巨石/井水/蛛网及三烟寺蒸笼均有运行时或明确排除，事件状态进入存档 schema v21。
- [x] 逐源审计并处置 81 项 NPC 行为标记：17 verified、31 adapted、15 deferred、13 excluded、4 alias、1 source_noop；10 项比试门槛、7 个脚本问答、3 项交换、3 项死亡钩子、战斗回调及进场攻击均有运行时或明确后续绑定，剧情状态进入存档 schema v22。
- [x] 审计三项区域物品行为标记：两条烤山猪肉来源均保留六口食物、锤兵器与吃完变骨头语义，2 adapted；青石绳子仅提供无剧情恢复的室内自毁命令，1 excluded。
- [x] 补齐小娟地牢营救/遇害与刘老农复仇、青石村玉佩/蒙汗药链；固定源复核确认陈天星荐书来自未导入的 `mudlib/u/cloud` 振远镖局，准确绑定 M7 遗漏区域；三烟寺入口继续依赖 M7 月迟山。
- [x] 完成四区自动端到端玩法验收，并保留 M7 尸体/遗漏区域及 M8 环境调度依赖。

验收：121 个房间、57 个 NPC 定义、65 项房间行为、81 项 NPC 行为和 3 项区域物品行为均有精确机检处置；绮云城堡/地牢、青石八卦阵与玉佩药物链、三烟寺蒸笼及水烟阁战斗可回归运行。拓扑、NPC 与物品台账均为 `complete`，状态进入存档 schema v22，M6 验收通过。

### M7：大型与特殊区域

区域：`latemoon`、`death`、`graveyard`、`jail`、`u/cloud`。

状态：完成。

- [x] 区域目录 schema v4 导入 139 个房间：晚月庄 74、幽冥地界 12、荒冢 2、牢狱 0、青云境 51；另保留 204 个非房间源文件，世界源房间总数达到 552。
- [x] NPC 目录 schema v4 导入 85 个新定义；65 个已放置定义展开为 101 个实例，另复用 M4 乞丐与 M5 驻军各 2 个，M7 世界共 105 个 NPC 实例。
- [x] 保留 78 条房间对象映射的 117 个源实例：69 条映射解析为 105 个 NPC 实例，9 条映射的 12 件固定物品进入存档 schema v23，无缺失对象引用。
- [x] 接通晚月庄/青云境、晚月庄/三烟寺、伏龙山/雪亭、朝阳峰/乔阴及朝阳峰/三烟寺五组固定源双向主干，闭合 M6 保留的三烟寺入口。
- [x] 处置静态导入缺陷：M4/M5 已拥有的乞丐/驻军定义不重复导入；三条错误携带物路径解析到固定提交真实文件，缺失 `houndbane` 明确省略且不伪造掉落。
- [x] 逐源审计并处置 77 项房间行为标记：50 verified、23 adapted、1 deferred、1 excluded、2 source_noop；31 个门端点、22 条查看细节、晚月舞步/浴池/花与衣物、幽冥五步路和投胎均有运行时或明确后续绑定，事件状态进入存档 schema v24。
- [x] 逐源审计并处置 183 项 NPC 行为标记：33 verified、51 adapted、50 deferred、38 excluded、10 alias、1 source_noop；师承、问答、交换、比试门槛和战斗行为均有运行时或 M8 调度处置。
- [x] 审计 22 项晚月庄、幽冥与青云境区域物品行为：9 verified、13 adapted；no_drop 转移限制、舞谱/手镯/荐书命令、五种组合补品及三种肉类残余均有精确机检处置。
- [x] 完成晚月庄蓝止萍师承、竹蜻蜓/手镯/舞曲谱/令牌链，以及振远镖局入门、忘忧草荐书、陈天星授业、北河渡船和青云人物交换。
- [x] 处置死亡、牢狱、复活与尸体依赖：幽冥投胎和当前直接掉落可运行；临时茶杯、尸体动画/腐败及环境调度准确绑定 M8 世界生命周期。
- [x] 完成五区自动端到端玩法验收，并保留 M8 世界调度与生命周期依赖。

验收：139 个房间、85 个新 NPC 定义、105 个世界 NPC 实例、77 项房间行为、22 项区域物品行为和 183 项 NPC 行为均有精确机检处置。晚月门派/线索、青云镖局荐书/渡河/交换及幽冥投胎可回归运行，存档进入 schema v25；三份 M7 台账均为 `complete`，M7 验收通过。

### M8：命令、任务与世界系统收口

状态：已完成。

- [x] 建立世界时钟与受限离线推进底座：存档 schema v26 写入本地保存时间；重载时按每现实分钟一个 10 分钟世界 tick、最多 8 小时结算。取嵌入保存时间与文件修改时间中较晚者，时钟回拨或未来时间不结算；离线仅推进世界时钟及饱食/饮水消耗，不结算战斗、修炼、休息、恢复或状态效果，避免本地时间与反复读档产生收益。
- [x] 接入首个持久临时对象生命周期：两条来源死岩蛭在产生后保存 240 tick 的到期时间；在线与离线世界时钟均会清除地面或行囊中的已腐败实例，三次食用与药效不变。
- [x] 接入通用尸体三阶段生命周期：死斗生成持久地面尸体，120/240/300 tick 后依次转为腐烂尸体、骸骨和消散；化尸粉可清除仍属尸体的前两阶段。尸体与来源容器均可嵌套存物，消散或化尸时会把其中物品留在原地，NPC 携带物仍按来源直接掉落，存档 schema 升至 v27。
- [x] 导入 72 个源 NPC 的 173 条环境消息到 `migration/catalog/npc-ambient.json`：当前房间中已存在的 NPC 按来源 `chat_chance` 在在线世界 tick 中说出原文；可安全执行的来源移动在同一实例位置层运行，日界时回到来源位置，安全非剧情实例死亡后按延迟刷新。没有单人运行落点的动作/回调不伪造效果；离线推进不触发环境消息。
- [x] 接入首批持久 NPC 位置与安全随机移动：存档 schema v28 按来源房间、NPC 与同房序号记录移动实例；16 组无商店、任务、师承、门禁依赖的来源 NPC（30 个静态实例）仅会在在线 tick 中沿同区域、无门、非动态的可用出口游走，并在日界归位。安全非剧情实例死斗后 72 个世界 tick 刷新并恢复其来源携带物；注释掉的来源 `random_move` 不执行，离线不移动。
- [x] 依据 `mudlib/adm/daemons/natured.c` 与 `adm/etc/nature/day_phase` 接入原版八段昼夜：225 个来源 `outdoors` 房间保留气候区字符串，并在地点描述显示对应天色；在线跨越阶段只向当前户外角色发送来源提示，离线推进不补发环境消息。源文件中的 `weather_msg` 没有消费调用点，未臆造天气数值或随机效果。
- [x] 依据 `mudlib/d/choyin/npc/judgeman.c` 与 `yamen_po.c` 接入单人本地司法状态机：现有累计通缉映射来源 `crime_lv`，在线 tick 依次推进县衙缉拿、铁链拘押、押回大堂与打板判决；拘押期间禁用移动等行动，仅开放来源十万文赎放，成功减三级罪名。状态进入 schema v29，离线推进不会缉拿或审判；原版仅面向其他在线玩家的 `charge` 命令继续排除。
- [x] 审计 `mudlib/cmds` 的 130 个命令并写入 `migration/overrides/m8-commands.json`：18 verified、30 adapted、0 deferred、80 excluded、2 source_noop。`attach`、`enchant`、`enforce`、`put`、`scribe`、`selflearn`、`steal`、`suicide` 与 `wimpy` 均以行动、战斗设置、确认弹层或本地存档完成单人适配；管理员、文件系统、驱动调试和在线多人命令明确排除，`chant` 与 `to` 本身为空实现不伪造功能。
- [x] 依据 `mudlib/u/cloud/npc/god.c`、15 个 `mudlib/quest/qlist*.c` 与 `adm/daemons/combatd.c` 接入朱鸿雪动态任务：`src/quests.rs` 在剔除来源注释后固定解析 220 条杀敌任务、84 个目标；206 条初始静态目标可发放，蛛网生成的蜘蛛另开启 3 条，11 条无活体实例的任务明确过滤。领取沿用大于一千实战经验、连续三次调阶和固定 factor=10；击杀按来源半数随机奖励、可用潜能 100 上限及负评价语义结算。截止时间绑定受限世界时钟，离线推进同样逾期，存档进入 schema v30；`m8-quests.json` 逐表登记覆盖和排除。
- [x] 依据 `mudlib/cmds/std/abandon.c` 接入遗忘技能：常规非战斗场景可从行动列表选择已学技能，确认后只删除该技能及其当前熟练进度；不返还已消耗潜能，重新学习从零开始。为保留来源 `delete_skill` 的语义，现有致能映射不会被自动解除，只是不再获得已删除技能的加成；若正修炼该技能则先停止修炼。确认弹层属于单人 TUI 的防误触适配，不改变存档 schema。
- [x] 依据 `mudlib/cmds/usr/skills.c` 接入武学总览：按来源稳定 ID 排序显示已学技能、`□` 致能标记、原版武学/知识等级评语、层级和当前熟练进度；`v` 打开并支持逐行、分页、首尾滚动。单人版仅查看自身，原命令的师徒、伴侣和巫师跨角色查询不进入本地存档。
- [x] 完成单角色身份档案：`i` 面板显示姓名、性别、名号、师门、师承、实战经验、评价和江湖状态；不伪造原版跨角色玩家目录。
- [x] 完成可安全的 NPC 日程、刷新与玩家遗体生命周期：日界归位、72 tick 安全刷新、遗体行囊、化尸/消散掉落和追魂符限时协击均持久化。来源 `weather_msg` 没有消费调用点，天气数值与无运行落点的随机脚本保持不伪造。
- [x] 固化离线策略：每现实分钟推进一个 10 分钟世界 tick、最多 8 小时，采用保存时间与文件修改时间较晚者并拒绝时钟回拨；离线只推进时钟、饱食/饮水、物品到期和限时任务，不产生战斗、修炼、恢复、状态、环境消息或 NPC 移动收益。

### M9：100% 验收与发布（进行中）

- [x] `es2-audit` 递归检查所有迁移 JSON；`discovered`、`structured`、`implemented` 与 `blocked` 均为发布阻断状态。
- [x] 运行时出口、NPC、固定物品和训练技能引用在测试及发布审计中逐项验证；三条固定源错误/外部出口只保留台账，不进入可行动世界图；黑松随机出口的全部候选均验证为真实地点。`snow.herbshop1`、`temple.broom1`、`temple.broom2` 是已登记的隐藏/重复来源房间，其余 549 间来源房间纳入区域可达性验收。
- [x] 220 条动态任务与 M8 台账、发放、逾期、完成、明确失败、奖励和存档升级回归绑定。
- [x] CI 重生成全部 17 个区域、四组 NPC、环境消息、物品和技能目录；Linux 与 Windows 各自运行全量终端测试和 Release 构建。
- [x] 标签发布随二进制附带 `content-coverage.json` 与 `migration-exclusions.json`。
- [ ] 等待 GitHub Actions 首次全量 Linux/Windows 运行通过后，将本里程碑标记为完成；人工区域游玩记录仍按第 6 节保留为发布前验收材料。

## 6. 测试策略

- **内容测试**：ID 唯一、出口目标存在、对象/技能引用有效、区域可达。
- **规则测试**：战斗、经济、状态、学习条件和任务状态机。
- **回归场景**：以固定随机种子运行关键任务和门派成长流程。
- **存档测试**：每个 schema 版本都能升级到当前版本。
- **TUI 测试**：58×20、80×24、120×30；普通、战斗、弹窗和长文本状态。
- **人工验收**：每个区域至少完成一次从入口到主要剧情终点的游玩记录。

## 7. 进度报告规则

每个里程碑完成后更新本文件，并生成 `migration/catalog/*.json`。报告同时给出：

- 源候选数。
- `structured` 数量。
- `implemented` 数量。
- `verified` 数量。
- `excluded` 数量及理由。
- 当前阻塞项。

在 M9 之前不得使用“100% 移植完成”的表述。

## 8. 当前执行记录

| 日期 | 工作 | 结果 |
| --- | --- | --- |
| 当前 | M0 可玩骨架 | 8 项测试、Clippy 和 Release 构建通过 |
| 当前 | 源仓库初步盘点 | 识别 560 房间、307 NPC、451 物品、70 技能、130 命令 |
| 当前 | M1 静态内容导入 | 26/26 房间达到 `structured`，8 组对象引用保留，20 项动态行为已验证 |
| 当前 | M1 运行时接入 | 26 个原版房间进入 TUI；内部出口全部有效且全图可达；2 个跨区出口等待后续区域 |
| 当前 | M2 物品目录 | 451/451 个候选达到 `structured`；74 条动态或运行时名称告警保留 |
| 当前 | M2 物品运行时 | 稳定 ID、实例、堆叠、负重、装备槽、耐久和地点掉落完成 |
| 当前 | M2 经济与状态 | 货币换算、买卖赠予、40 种食物、11 种饮品、药物与状态时钟完成；存档升级到 schema v4 |
| 当前 | M2 共享对象审计 | 13/13 项有明确处置：6 verified、3 deferred、4 excluded；M2 验收通过 |
| 当前 | M3 技能目录 | 70/70 技能、114 个招式、11 位掌门达到 structured；稳定技能 ID 与 schema v5 完成 |
| 当前 | M3 训练与战斗 | 师承、学习、映射、练习、读书、修行、比试/死斗、逃跑、杀气与通缉完成 |
| 当前 | M3 动态行为审计 | 143/143 行为标记有处置；33 个脚本为 20 verified、10 deferred、3 alias；M3 验收通过 |
| 当前 | M4 静态区域导入 | `city` 55、`snow` 38、`temple` 27、`canyon` 27 个房间达到 `structured`；无空房名或空描述 |
| 当前 | M4 运行时地图 | 173 个 M1/M4 源房间进入世界；五区静态主干、黄石隘口铁索和尚书府院墙移动完成 |
| 当前 | M4 拓扑审计 | 穿林入口适配、铁索/院墙脚本移动与黑市口令入口完成；4 项固定源缺陷有明确处置 |
| 当前 | M4 房间事件批次 1 | 60 个房间 107 项警告建立精确基线；8 个机制的 22 项警告为 2 verified、5 adapted、1 excluded，存档进入 schema v12 |
| 当前 | M4 门状态 | 区域目录 schema v4 保留 36 个 M4 门定义；35 项门警告为 26 verified、9 excluded，14 对有效门进入存档 schema v13 |
| 当前 | M4 房间细节 | 32 项警告全部处置：32 个房间、42 条细节，16 verified、16 adapted；门描述绑定实时状态 |
| 当前 | M4 动态房间审计 | 107/107 项有处置；除门与细节外的 40 项为 12 verified、19 adapted、1 source-noop、1 deferred、7 excluded |
| 当前 | M4 NPC 目录 | 74 个定义；去除注释后的 71 条静态映射展开为 103 个实例，加入 2 个确定化随机守卫后共 61 个运行时定义、105 个实例；158 项行为标记有台账 |
| 当前 | M4 商店运行时 | 多 NPC 房间与稳定 NPC ID 完成；9 个原版商人、27 种货品达到 verified |
| 当前 | M4 NPC 问答 | 75 个源主题进入 schema v2 目录；50 个静态主题和 10 个脚本主题可用，105 个房间主题引用；18 个脚本为 4 verified、6 adapted、1 source_noop、7 excluded |
| 当前 | M4 书院事件 | 魏无极学费与识字上限、刘安禄身份死斗与刀谱掉落完成 |
| 当前 | M4 物品交换 | 19/19 项有处置：8 verified、2 adapted、4 deferred、5 excluded；城隍庙捐献完成，延期项准确归入 M6/M8 |
| 当前 | M4 条件入口 | 黄石黑市、军营、真假印鉴古剑链、京城酒楼和尚书府门槛完成；状态进入存档 schema v7 |
| 当前 | M4 NPC 自定义命令 | 11/11 项有处置：9 verified、1 adapted、1 excluded；废园令牌、守城兵交验和一次性北门通行完成，存档进入 schema v11 |
| 当前 | M4 NPC 比试门槛 | 9/9 项有明确处置；8 个已放置 NPC 支持强制死斗、同门限定或源拒绝；存档进入 schema v8 |
| 当前 | M4 NPC 师承 | 8/8 项有明确处置：5 verified、1 deferred、2 excluded；5 个授业 NPC 的 37 项原版课程可运行 |
| 当前 | M4 NPC 战斗档案 | schema v4 保留 73 个档案、316 项技能、94 项映射、21 项修正；61 个运行时定义的 105 个实例均可战斗，存档进入 schema v10 |
| 当前 | M4 NPC 携带物 | 55 个定义的 93 件初始物品结构化；43 个已放置定义的 76 个房间实例首次死斗后按源掉落，126 件运行时战利品可结算 |
| 当前 | M4 NPC 战斗消息 | 25 个定义的 87 条消息按类型结构化；16 个静态 NPC 与 2 个确定化守卫执行当前系统可表达的源战斗行为，M7/M8 依赖明确保留 |
| 当前 | M4 刘老农剧情 | 救出后护送、衡冰剑与过招要旨奖励、父女离场及遇害复仇分支完成；存档进入 schema v9，M6 迁回原版地牢入口 |
| 当前 | M4 房间对象 | 去除注释后的 77 条映射和 110 个源实例全部处置；105 个 NPC 实例与 5 件固定房间物品运行，物品初始化进入存档 schema v14 |
| 当前 | M4 验收 | 房间、NPC、对象、107 项动态警告和 158 项 NPC 标记均有机检处置；后续依赖准确归入 M6/M7/M8，验收通过 |
| 当前 | M5 静态区域 | 119 个房间、去除注释后的 64 条对象映射和 133 个源实例进入 schema v4 目录；三地接入主世界，10 件固定物品进入存档 schema v15 |
| 当前 | M5 静态 NPC | 49 个定义、46 个已放置定义、123 个房间实例；商店/静态问答、战斗档案/消息、携带物与掉落进入通用运行层 |
| 当前 | M5 通用房间行为 | 23 项警告有处置：19 项细节为 16 verified、3 adapted；4 项门为 2 verified、2 excluded |
| 当前 | M5 黑松脚本路径 | 29 项警告有处置：随机洞窟/松林、九条攀爬路径和藤蔓分支为 28 adapted、1 source-noop |
| 当前 | M5 乔阴事件批次 1 | 9 项警告有处置：绝壁、云台、井水和石狮为 1 verified、8 adapted，事件状态进入存档 schema v16 |
| 当前 | M5 房间行为收口 | 最后 12 项为 10 adapted、1 deferred、1 excluded；黑松埋骨/封寨、借书、桃林与正式掌门入口可运行，73/73 房间警告均有处置 |
| 当前 | M5 NPC 战斗门槛 | 3 项比试门槛和 1 项死斗门槛为 3 verified、1 adapted；酒楼报官进入通缉状态 |
| 当前 | M5 NPC 行为审计 | 59/59 项标记有处置：14 verified、38 adapted、6 deferred、1 excluded；6 个脚本问答、2 项交换与2项死亡钩子可运行，存档进入 schema v18 |
| 当前 | M5 区域敌人与物品 | 战斗概率回退、土匪动态援军、金银花蛇蛇毒、三类岩蛭尸体补品完成；13/13 区域物品标记为 12 adapted、1 alias |
| 当前 | M5 门派与验收 | 缚仙绳/仙丹链和骆云舟桃林试炼完成，动态援军与试炼状态进入 schema v19；三地自动端到端回归通过，M7/M8 依赖保留，M5 验收通过 |
| 当前 | M6 静态区域 | 121 个房间、60 条对象映射和 105 个源实例进入 schema v4 目录；三条现有主干接入，两个固定源缺失引用明确登记 |
| 当前 | M6 静态 NPC 与物品 | 57 个 NPC 定义、92 个房间实例、1 个商人的 6 项货品及 11 件固定物品进入通用运行层；存档进入 schema v20 |
| 当前 | M6 房间行为 | 65/65 项有处置：29 verified、34 adapted、2 excluded；通用门/细节与城堡、八卦阵、井水、蛛网、蒸笼状态机完成，存档进入 schema v21 |
| 当前 | M6 区域物品 | 3/3 项有处置：两条猪肉 consume hook 为 adapted，绳子自毁命令为 excluded；`m6-items` 台账完成 |
| 当前 | M6 NPC 行为 | 81/81 项有处置：17 verified、31 adapted、15 deferred、13 excluded、4 alias、1 source_noop；比试/死斗门槛、战斗回调与进场攻击完成 |
| 当前 | M6 共享剧情与验收 | 小娟/刘老农、青石玉佩/蒙汗药链完成，存档进入 schema v22；陈天星荐书纠正绑定 M7 `u/cloud`，四区自动回归通过，M6 验收完成 |
| 当前 | M7 静态区域 | 139 个房间、204 个非房间文件、78 条对象映射和 117 个源实例进入 schema v4 目录；五组跨区固定源主干接通 |
| 当前 | M7 静态 NPC | 85 个新定义的 101 个实例加 4 个跨阶段复用实例；8 个商人的 29 项货品、67 个问答主题、85 个战斗档案与 122 件携带物进入通用运行层 |
| 当前 | M7 固定物品与存档 | 9 条映射的 12 件固定房间物品按源数量初始化；`u.cloud` 前缀纳入幂等迁移，存档进入 schema v23 |
| 当前 | M7 房间行为 | 77/77 项有处置：50 verified、23 adapted、1 deferred、1 excluded、2 source_noop；晚月、幽冥与青云房间事件完成，存档进入 schema v24 |
| 当前 | M7 区域物品 | 22/22 项有处置：9 verified、13 adapted；no_drop、舞谱/手镯/荐书命令、组合补品和肉类残余进入 `m7-items` 台账 |
| 当前 | M7 NPC 行为 | 183/183 项有处置：33 verified、51 adapted、50 deferred、38 excluded、10 alias、1 source_noop；门派、问答、交换、比试与战斗层完成 |
| 当前 | M7 剧情与验收 | 晚月线索/令牌、振远镖局荐书、陈天星授业、北河渡船与青云交换完成，存档进入 schema v25；五区自动回归通过，M8 生命周期依赖保留，M7 验收完成 |
| 当前 | M8 世界时间、生命周期与身份 | 存档 schema v31：v26 记录保存时钟，v27 增加通用尸体阶段，v28 保存安全随机移动位置，v29 保存乔阴县司法状态，v30 保存朱鸿雪动态悬赏，v31 保存嵌套容器、符咒、偷窃/刷新状态、战斗设置和单角色身份资料；离线每现实分钟推进一个 10 分钟世界 tick、上限 8 小时，时钟回拨不结算。尸体、化尸、容器内容和安全 NPC 日界归位/72 tick 刷新均不会吞没物品；225 个 `outdoors` 房间显示八段天色，在线跨阶段才提示；离线不产出战斗、修炼、恢复、状态、环境消息或 NPC 移动收益；乔阴司法维持本地缉拿、拘押、押解、判决和十万文赎放 |
| 当前 | M8 朱鸿雪动态任务 | `m8-quests.json` 覆盖 15 张来源表的 220/220 条活动任务：206 条初始静态目标可发放，3 条蜘蛛任务在动态生成后可发放，11 条无活体实例任务过滤；来源阶位、时限、连续完成、奖励范围、潜能上限、负评价与 v29→v30 存档升级均有回归覆盖 |
| 当前 | M8 遗忘技能 | `abandon` 以行动项与二次确认弹层适配：删除技能及当前熟练进度，不返还潜能；来源保留的致能映射语义和修炼中止均有回归测试，不需存档升级 |
| 当前 | M8 武学总览 | `skills` 以 `v` 面板适配：来源排序、`□` 致能、武学/知识等级评语、层级和熟练进度可滚动查看；跨角色查询按单人范围排除 |
| 当前 | M8 命令、战斗与身份 | `m8-commands.json` 覆盖 130/130 条来源命令：18 verified、30 adapted、0 deferred、80 excluded、2 source_noop。九条原延期玩家命令均以行动、容器、符咒、延时偷窃、战斗设置、投胎或双重删档确认完成；`i` 为本地身份档案，跨角色目录仍按单人范围排除 |
| 2026-08-29 | M9 发布验收门禁 | 新增 `es2-audit`：递归阻断四种中间迁移状态，核对运行时引用、任务台账和覆盖率契约，并生成完整排除清单；CI 重生成全部目录，Linux/Windows 均测试并构建；标签发布附带两份 JSON 元数据。等待首次托管 CI 全量通过后收口 M9。 |
